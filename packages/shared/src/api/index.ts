// Platform-agnostic API client: request helpers plus one typed function per
// backend endpoint. Configure platform behavior (base URL, storage,
// unauthorized handling, fetch) via `configureApiClient` — see `./client`.
export * from './client';
