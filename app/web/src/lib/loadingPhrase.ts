// Moved to the shared package so the web app and the future mobile app share
// one implementation. Re-exported here so existing `lib/loadingPhrase` imports resolve.
export { loadingPhrase } from '@personal-assistant/shared/utils';
