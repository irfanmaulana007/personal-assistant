.PHONY: build dev deps clean

# Build the client
build:
	pnpm build

# Development server
dev:
	pnpm dev

# Install dependencies (pnpm installs the whole workspace from any package dir)
deps:
	pnpm install

clean:
	rm -rf dist
