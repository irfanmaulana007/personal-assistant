# Encryption key for OAuth2 token storage (32 bytes, base64 encoded)
# Generate with: openssl rand -base64 32
ENCRYPTION_KEY=

# Override owner WhatsApp JID (optional, can also set in config.yaml)
OWNER_JID=

# Override database path (optional, defaults to data/assistant.db)
DB_PATH=

# Override log level (optional: debug, info, warn, error)
LOG_LEVEL=

# Web interface password (required when web is enabled)
WEB_PASSWORD=
